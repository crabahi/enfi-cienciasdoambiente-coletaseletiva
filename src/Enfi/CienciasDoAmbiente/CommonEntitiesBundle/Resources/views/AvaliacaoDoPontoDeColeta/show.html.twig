{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AvaliacaoDoPontoDeColeta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pontodecoletaid</th>
                <td>{{ entity.pontoDeColetaId }}</td>
            </tr>
            <tr>
                <th>Usuarioid</th>
                <td>{{ entity.usuarioId }}</td>
            </tr>
            <tr>
                <th>Timestamp</th>
                <td>{{ entity.timestamp }}</td>
            </tr>
            <tr>
                <th>Comentario</th>
                <td>{{ entity.comentario }}</td>
            </tr>
            <tr>
                <th>Nota</th>
                <td>{{ entity.nota }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('manage_avaliacaodopontodecoleta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('manage_avaliacaodopontodecoleta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
